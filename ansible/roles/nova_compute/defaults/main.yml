#SPDX-License-Identifier: MIT-0
---
# defaults file for roles/nova_compute

# Ceph RBD backend for Nova ephemeral disks (decision #31)
ceph_vms_pool: vms

# rbd_secret_uuid moved to group_vars/all.yml — it is a cluster-wide identifier
# shared by Nova ephemeral and Stage 6 Cinder (the one libvirt secret on each
# compute serves both), not a nova_compute-specific value.
